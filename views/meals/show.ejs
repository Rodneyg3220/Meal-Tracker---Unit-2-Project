<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Meal Tracker</title>

    <%- include('../partials/header') %>

<section id="show-page">
  <div>Meal Name: </div>
  <div><%= meals.meal %></div>
  <div>Ingredients: </div>
  <div><%= meals.ingredients %></div>
  <div>Do you have ingredients? </div>
  <div><%= meals.haveIngredients %></div>
  <div>Total Calories: </div>
  <div><%= meals.nutrition ? meals.nutrition.calories : 'N/A' %></div>
  <div>Protein Count: </div>
  <div><%= meals.nutrition ? meals.nutrition.protien : 'N/A' %></div>
  <div>Sugar Total: </div>
  <div><%= meals.nutrition ? meals.nutrition.sugar : 'N/A' %></div>
  <div>Day of Meal:<div>
  <div><%= meals.nutrition ? meals.nutrition.mealDay : 'N/A' %></div>
  

</section>
 
  <form id="add-meals-form" method="POST"
  action="/meals/<%= meals._id %>/nutrition">
  <br>
</form>
<form id="delete-meal" method="POST"
action="/meals/<%=meals._id %>?_method=DELETE" >
  <button type="submit">Delete Meal</button>
</form>
</section>

<br>
</html>